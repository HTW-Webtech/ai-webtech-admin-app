# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  secret_key_base: 9c0812c4086a2e48a6b5b5acc5bbfdfa4985f09c58d335990e84ddc1fa8ba415ebc4aee541b18d64005c0ef274076eb8e3fe8c156b0b10fbcd29762bcf09a92e
  git_ssh_host: 'localhost:2222'

test:
  secret_key_base: c5f5b7dca97a6c0463839a205941c6c12096cb618f19352c3a62bb66fdd47e6428621fccbea47a2ecc5cbdaeb76d5c3b28373d937abc3b29f3d8c7974e77cfef

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <%= ENV['APP_SECRET_KEY_BASE'] %>
  secret_base: <%= ENV['APP_SECRET_KEY_BASE'] %>
  git_ssh_host: <%= ENV['GIT_SSH_HOST'] %>
# FIXME: Secret base behavior
